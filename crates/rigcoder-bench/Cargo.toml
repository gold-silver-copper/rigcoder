[package]
name = "rigcoder-bench"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Runs rigcoder against Terminal-Bench task directories through Docker, and the self-improvement loop over those scores."

[[bin]]
name = "rigcoder-bench"
path = "src/main.rs"

[dependencies]
clap = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = "0.9"
